<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Weather Info</a>
    </div>

    <div id="navbar">
      <ul class="nav navbar-nav navbar-right">
        <%= form_tag [:welcome, :location], role: 'form', class: 'navbar-form navbar-left', method: :post, remote: true do |f|%>
            <div class="form-group">
              <%= text_field_tag :city, nil, placeholder: 'Enter city name', class: 'form-control', required: true%>
            </div>
            <%= submit_tag 'Submit', class: 'btn btn-default'%>
        <% end %>
      </ul>
    </div>
  </div>
</nav>